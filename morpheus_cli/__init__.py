"""Morpheus CLI — command-line surface for the Morpheus knowledge-graph fork.

Wraps ``graphiti_core`` directly (no MCP round-trip), absorbs the most
useful Graphify graph-operations commands, and adds a bulk-add adapter
on top of the MCP-parity surface.
"""

__version__ = '0.1.0'
